<ui:ContentDialog x:Class="StockTill.Controls.AddCategoryDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:controls="clr-namespace:StockTill.Controls"
    Title="新增商品分类"
    d:DesignHeight="756" d:DesignWidth="548"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimaryButtonClick"
    PrimaryButtonText="完成"
    CloseButtonText="取消"
    mc:Ignorable="d">
    <StackPanel>
        <TextBox ui:ControlHelper.Header="商品分类" Text="{Binding category, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,16" />
        <ui:ProgressBar x:Name="DialogProgress" IsIndeterminate="True" Visibility="Collapsed" Margin="0,24,0,0" />
    </StackPanel>
</ui:ContentDialog>