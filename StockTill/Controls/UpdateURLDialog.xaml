<ui:ContentDialog x:Class="StockTill.Controls.UpdateURLDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:controls="clr-namespace:StockTill.Controls"
    Title="编辑更新服务器"
    d:DesignHeight="756" d:DesignWidth="548"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimaryButtonClick"
    PrimaryButtonText="保存"
    CloseButtonText="取消"
    mc:Ignorable="d">
    <StackPanel>
        <TextBox ui:ControlHelper.Header="更新服务器" Text="{Binding UpdateURL, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" />
        <ui:ProgressBar x:Name="DialogProgress" IsIndeterminate="True" Visibility="Collapsed" Margin="0,24,0,0" />
    </StackPanel>
</ui:ContentDialog>