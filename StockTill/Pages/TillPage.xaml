<ui:Page x:Class="StockTill.Pages.TillPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StockTill.Pages"
      xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="收银出库">
    <Grid Margin="56,36,56,36">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Margin="0,0,0,20">
            <Bold>提示：</Bold>如果同一个商品有多件，可以直接修改数量，而无需扫描或输入多次。
        </TextBlock>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBox x:Name="IdBox" ui:ControlHelper.Header="扫描或输入商品编号，按回车确认：" ui:ControlHelper.PlaceholderText="商品编号" Loaded="IdBox_Loaded" KeyDown="IdBox_KeyDown" Width="340" Margin="0,0,8,0"/>
            <Button x:Name="EraseButton" Click="EraseButton_Click" VerticalAlignment="Bottom" Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.EraseTool}" />
                    <TextBlock Text=" 清空所有" />
                </StackPanel>
            </Button>
            <TextBlock Text="{Binding CountText, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Bottom" Height="24" />
        </StackPanel>
        <DataGrid Grid.Row="2" x:Name="TillGrid" CanUserAddRows="False" Loaded="TillGrid_Loaded" CellEditEnding="TillGrid_CellEditEnding" />
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <TextBlock Margin="24">
                <Bold>总金额：</Bold><Run Text="{Binding TotalPrice, UpdateSourceTrigger=PropertyChanged}" /> 元
            </TextBlock>
            <Button x:Name="TillButton" Content="出库" Style="{DynamicResource {x:Static ui:ThemeKeys.AccentButtonStyleKey}}" Click="TillButton_Click" />
        </StackPanel>
    </Grid>
</ui:Page>
