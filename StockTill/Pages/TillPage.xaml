<ui:Page x:Class="StockTill.Pages.TillPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StockTill.Pages"
      xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="扫码出库">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBox x:Name="IdBox" ui:ControlHelper.Header="扫描或输入商品编号，按回车确认：" ui:ControlHelper.PlaceholderText="商品编号" Loaded="IdBox_Loaded" KeyDown="IdBox_KeyDown" Width="320" Margin="0,0,8,0"/>
            <Button x:Name="EraseButton" Content="清空所有" Click="EraseButton_Click" VerticalAlignment="Bottom" Width="Auto" Margin="0,0,8,0"/>
        </StackPanel>
        <DataGrid x:Name="TillGrid" Grid.Row="1" Loaded="TillGrid_Loaded" CellEditEnding="TillGrid_CellEditEnding" CanUserAddRows="False" />
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Margin="24">
                <Bold>总金额：</Bold><Run Text="{Binding TotalPrice, UpdateSourceTrigger=PropertyChanged}" /> 元
            </TextBlock>
            <Button x:Name="TillButton" Content="出库" Style="{DynamicResource {x:Static ui:ThemeKeys.AccentButtonStyleKey}}" Click="TillButton_Click" />
        </StackPanel>
    </Grid>
</ui:Page>
